# Placeholder photo assets

The create-recipe form falls back to a default photo here when no image has been
uploaded yet.

Expected files (still to be added):

- `cocktail-default.jpg` shown when Category = Cocktail
- `food-default.jpg` shown when Category = Food

These will be generated as AI illustrations and dropped in alongside this README.
Until they exist the upload area will show a broken-image icon, which is
harmless because the placeholder is purely cosmetic.
